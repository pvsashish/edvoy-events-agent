# Features Index

**Last updated:** 2026-07-06 (Feature-context now steers the pipeline; platform toggle follows connected sheets; image-preview lightbox with prev/next + keyboard) · earlier: 2026-07-03 (Mobile/tablet responsive fixes; Space switcher incl. Scout scoping; History thumbnails on Cloudflare R2 + auto-migration; Anthropic Sonnet 4.6 only; per-generate cost chip; accuracy overhaul; merged table, paste, toggle lock)

| Feature | Doc | Status | Key Files |
|---------|-----|--------|-----------|
| Screenshot Upload | [UPLOAD_FLOW.md](upload/UPLOAD_FLOW.md) | complete | `public/app.jsx` (addFiles, toDataUrl) |
| Video Upload + Scene-change Frame Extraction | [UPLOAD_FLOW.md](upload/UPLOAD_FLOW.md) | complete | `public/app.jsx` (`extractVideoFrames`) — content-driven: densely samples the video, keeps one frame per distinct screen (normalised RGB diff > 0.075 vs last kept; first always kept), skips dead time. Frame count = number of distinct screens, capped at 12. Non-finite duration (webm/`Infinity`) resolved by seeking to end first. |
| Image Preview Lightbox (prev/next) | — | complete | `public/app.jsx` (`AttachmentPreviewModal`) — click any attachment (Event Generator) or Specs History thumbnail to open a fullscreen preview. Left `‹` / right `›` arrow buttons **and** keyboard ←/→ step through the set (clamped at ends, hidden when only 1 image), with an `n of m` counter; Escape closes. Generator opens over the `attachments` list; History opens over all visible-history items that have a thumbnail (thumbnail `onClick` uses `stopPropagation` so it doesn't also load the spec). State: `preview = { items, index }`. **Videos play** in-lightbox via `<video controls>` on the retained `videoUrl` blob (frame-thumbnail still fallback only when no playable video, e.g. saved-history videos). |
| GA4 / Amplitude Toggle | — | complete | `public/app.jsx`, `prompts/ga4.js`, `prompts/amplitude.js` — **the toggle follows the connected sheets for the active Space**: a platform is enabled only when its tracking sheet has synced `data` (`sheetConfig[space]?.[p]?.data`). One connected → that platform auto-selected, the other hard-disabled (greyed, `not-allowed`, "Connect a … sheet to enable" tooltip). Both connected → user picks. Neither → both disabled and Generate blocked with a "Connect a tracking sheet" hint. A `useEffect([space, sheetConfig])` auto-switches the selection off a now-disabled platform; deps deliberately exclude `platform` so a history restore isn't clobbered. |
| Feature Context Input | [ANALYSIS_FLOW.md](analysis_pipeline/ANALYSIS_FLOW.md) | complete | `public/app.jsx` (textarea), `api/analyze.js` + `prompts/identify.js` + `prompts/match.js` — optional PM flow hint, threaded into all 3 pipeline steps to steer new/ambiguous events; never overrides sheet-matched (verbatim) events. Blank = zero behaviour change. |
| AI Analysis (3-step pipeline) | [ANALYSIS_FLOW.md](analysis_pipeline/ANALYSIS_FLOW.md) | complete | `api/analyze.js`, `prompts/*` — **Anthropic Claude Sonnet 4.6 only, temperature 0**. Reuse-before-inventing, matched-event param hints, no screen-view events, dedup. `from`-as-minimum is **GA4-only** — Amplitude properties are optional and `from` is hard-filtered server-side. Returns `usage` (tokens + $ cost) → per-generate cost chip + cumulative API Usage card in sidebar (**DB-persisted** via `edvoy_usage`/`api/usage.js` as of 2026-07-07 — survives cache clear; localStorage is an offline cache). Groq + Gemini removed 2026-06-30 |
| Cross-Platform Consistency | [ANALYSIS_FLOW.md](analysis_pipeline/ANALYSIS_FLOW.md) | complete | `api/analyze.js` (sessionEvents), `public/app.jsx` (eventsPlatform state) |
| Events Table Output | — | complete | `public/app.jsx` |
| Inline Table Editing | — | complete | `public/app.jsx` (handleCellChange) |
| Copy as TSV | — | complete | `public/app.jsx` (copyTsv) |
| Export CSV | — | complete | `public/app.jsx` (downloadCsv) |
| Copy JSON | — | complete | `public/app.jsx` (copyJson — _rowId stripped from output) |
| Specs History Log | [SPECS_HISTORY_FLOW.md](specs_history/SPECS_HISTORY_FLOW.md) | complete | `api/history.js`, `api/db.js`, `public/app.jsx` — Space-scoped; thumbnails on Cloudflare R2 (`history/<id>`) with a self-healing client-side migration for pre-existing localStorage-only thumbnails |
| Specs History Pagination | [SPECS_HISTORY_FLOW.md](specs_history/SPECS_HISTORY_FLOW.md) | complete | `public/app.jsx` (historyPage state, 5 items/page) |
| Space Switcher | — | complete | `public/app.jsx` (`SPACES` const, `space`/`setActiveSpace`), `api/db.js`, `api/history.js`, `api/screens.js` — sidebar "Space" dropdown (`Edvoy Student` / `Edvoy Connect`) scopes tracking-sheet config, Specs History, and Scout so the two Edvoy surfaces never share data. Switching space also closes any open tracking-sheet edit box. |
| Naming Converter Tab | [NAMING_CONVERTER_FLOW.md](naming_converter/NAMING_CONVERTER_FLOW.md) | complete | `public/app.jsx` (getValidationError, autoCorrect) |
| Batch Naming Converter | [NAMING_CONVERTER_FLOW.md](naming_converter/NAMING_CONVERTER_FLOW.md) | complete | `public/app.jsx` |
| Workspace Reset Tool | — | complete | `public/app.jsx` (resetWorkspace) |
| Duplicate Generation Prevention | — | complete | `public/app.jsx` (generatedAttachments state lock + eventsPlatform check) |
| File Type Validation | [UPLOAD_FLOW.md](upload/UPLOAD_FLOW.md) | complete | `public/app.jsx` (addFiles — MIME + extension fallback) |
| Mobile Responsive Layout | — | complete | `public/index.html` (hamburger nav, single-col grid ≤768px), `public/app.jsx` (`<main>` needs `minWidth:0` as a flex child — protects every tab, not just Scout). Fixed 2026-07-03: a global `aside { transform: translateX(-100%) }` rule meant only for the nav drawer was also hijacking Scout's unrelated `<aside>` (event rail), shoving it off-screen on mobile/tablet — rescoped to `.app-sidebar`. Scout's canvas+rail grid now stacks below 768px (`minmax(0,1fr)`, not bare `1fr` — the latter still respects content's min-width). |
| Sample Value Normalisation | [ANALYSIS_FLOW.md](analysis_pipeline/ANALYSIS_FLOW.md) | complete | `api/analyze.js` (is_clicked → true/false, *_id → dynamic value) |
| TrackingSheets Cards | — | complete | `public/app.jsx` (`TrackingSheetCard` component) — compact single-row card with provider logo, connected dot, icon-only Edit + Re-sync buttons, hover lift. Wired to existing `syncSheet` / `sheetConfig` state. |
| Manrope / Inter font system | — | complete | `public/index.html` — Manrope for headings/labels/display, Inter for buttons/nav/body. `--font-display: Manrope`, `--font-body: Inter`. |
| Scout — Event Map | [SCOUT_FLOW.md](scout/SCOUT_FLOW.md) | complete | `api/screens.js`, `api/db.js`, `public/app.jsx` — 227 events across 43 screens (147 GA4 + 80 Amplitude), all `edvoy-student` space. Unified card, images on Cloudflare R2 (loaded on demand with **hover-prefetch** + a branded loading animation — shimmer + gradient ring), scoped by Space. **Search** is normalized + word-order-independent (strips spaces/hyphens/underscores; every query word must appear in the screen or a single event name), with a Clear button next to Search that also resets the canvas. **Selection:** click an event to select, click again to deselect (toggle), "No event selected" empty state. **Pagination:** dense-pack, orphan-safe (~10 events/page, never strands <3 of a category). |
| Merged events table | — | complete | `public/app.jsx` — Category + Event Name cells vertically merged (rowSpan) across an event's param rows, like the tracking spreadsheet. `handleGroupFieldChange` propagates merged-cell edits to the group. **TSV + CSV export (`mergedExportRows`) blanks the repeated Category + Event Name on continuation rows** so a paste into the sheet matches the merged layout. Copy JSON keeps full objects (valid structured data). |
| Paste-to-upload | — | complete | `public/app.jsx` — ⌘V an image from the clipboard (iPhone screenshot → Mac) into the same `addFiles` flow. Generator tab only; ignores text paste. Drop zone auto-focuses so ⌘V works without clicking first. |
| Platform toggle lock | — | complete | `public/app.jsx` — GA4/Amplitude toggle `disabled` while `loading \|\| processing` (prevents switching mid-generation) **and when that platform's sheet isn't connected** for the active Space (see "GA4 / Amplitude Toggle" row). Inactive/disabled option dimmed to 0.4. |
